import 'package:flutter/material.dart';
class Adds_page extends StatefulWidget {
  const Adds_page({Key? key}) : super(key: key);

  @override
  State<Adds_page> createState() => _Adds_pageState();
}

class _Adds_pageState extends State<Adds_page> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Adds page')),
    );
  }
}